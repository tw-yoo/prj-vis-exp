// Re-export the existing collision-aware label placement helper. This is a
// design-rule helper (not lifecycle code), so reusing keeps the visual rule
// consistent across the engine.
export { placeOperationTextLabel } from '../../operation-next/textPlacement'
